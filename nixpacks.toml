[phases.setup]
nixPkgs = [
  "python311",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "libffi",
  "fontconfig",
  "freetype",
]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
